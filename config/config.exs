import Config

# Wiregrid does not require global application configuration. Instances are
# configured explicitly through Wiregrid.start_instance/2 so multiple isolated
# runtimes can coexist in one VM.
