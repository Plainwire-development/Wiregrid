window.WG_SEARCH = [
  {href: "index.html", title: "Front", text: "Manual 1.0 Wiregrid Wiregrid is a BEAM realtime library for sessions, subscriptions, fanout, presence, rooms, acknowledgements, reconnects, storage, rate limits, and optional clustering. Elixir is the main API. The same runtime is reachable from Erlang through wiregrid_api , from Gleam and LFE bindings, from a Cowboy WebSocket handler, and from non-BEAM programs through a versioned TCP protocol and a C client. The library keeps product rules outside itself. Users, roles, message schemas, and the product database stay in the embedding application. Wiregrid validates its own inputs, bounds its o"},
  {href: "install.html", title: "Install", text: "Setup Install Elixir stays a Mix dependency of the application that runs the node. The C library, the browser kit, and the Gleam, LFE, and Erlang sources are installed by scripts/install.sh onto a prefix. Mix compiles the BEAM files inside the host application. Mix def deps do [ {:wiregrid, \"~> 1.0\"} ] end mix.exs requires elixir: \">= 1.17.0\" . The application starts :crypto , :logger , :public_key , and :ssl , and the application callback is Wiregrid.Application . Runtime dependencies are empty on purpose. These packages are declared only: :test : Package Constraint Used for postgrex ~> 0.22."},
  {href: "chat.html", title: "Chat façade", text: "Application API Chat façade Wiregrid.Chat is the opinionated path for channel chat, direct messages, typing, reactions, edits, deletes, and read markers. It calls the same runtime as Wiregrid.publish/4 . There is no second engine and no second set of sessions. Defining the module defmodule MyApp.Chat do use Wiregrid.Chat, instance: :chat, options: [profile: :small, authorizer: MyApp.Auth], chat: [max_message_bytes: 8_192] end instance is required. options is the keyword list passed to Wiregrid.Config and defaults to [] , which selects the :balanced profile. chat is validated by Wiregrid.Chat.C"},
  {href: "sessions.html", title: "Sessions and resume", text: "Runtime Sessions and resume A session is a bounded attachment between a user id, an owner pid, and the instance’s ETS indexes. The owner is the process that receives {:\"$wiregrid\", envelope} . Cowboy, the foreign gateway, and Wiregrid.Consumer.Worker are owners. An ordinary Elixir process can be one too. Starting an instance {:ok, _pid} = Wiregrid.start_instance(:chat, profile: :small) # or, inside a supervision tree: Wiregrid.instance_child_spec(:chat, profile: :balanced) start_instance/2 builds config, then starts a permanent child under Wiregrid.InstanceSupervisor . Starting an instance tha"},
  {href: "publish.html", title: "Topics, publish, dispatch", text: "Routing Topics, publish, and dispatch A topic is a tuple. Fanout walks the ETS edges for that tuple. Storage, when asked to persist, uses the same tuple as the stream key. Topic shapes Tuple Constructor Foreign text {:user, id} Wiregrid.Topic.user/1 user:ada {:channel, id} Wiregrid.Topic.channel/1 general or channel:general {:thread, id} Wiregrid.Topic.thread/1 thread:42 {:room, id} Wiregrid.Topic.room/1 room:lobby {:game, id} Wiregrid.Topic.game/1 game:… {:document, id} Wiregrid.Topic.document/1 document:… {:custom, namespace, value} Wiregrid.Topic.custom/2 custom:ops:desk The whole term must"},
  {href: "delivery.html", title: "Delivery and acknowledgements", text: "Backpressure Delivery and acknowledgements Fanout does not call process_info/2 on each recipient. Each session has a counter Wiregrid maintains, and each durable send inserts a reservation {session_id, delivery_id} before the message is placed in the owner mailbox. The envelope receive do {:\"$wiregrid\", envelope} -> envelope.instance envelope.session_id envelope.delivery_id envelope.topic envelope.class envelope.event Wiregrid.ack(envelope.instance, envelope.session_id, envelope.delivery_id) end The message is always the two-tuple {:\"$wiregrid\", envelope} . The map always has instance , sessio"},
  {href: "presence.html", title: "Presence, rooms, signals", text: "Presence Presence, rooms, signals, typing, receipts These features share the session and the authorizer. They do not introduce a second routing table. Ephemeral broadcasts (typing, activity) can be dropped at the soft queue. Signals published as durable room events follow the durable rules. Presence :ok = Wiregrid.set_presence(:chat, session_id, :online, %{}) :ok = Wiregrid.set_status(:chat, session_id, :idle) snapshot = Wiregrid.presence(:chat, \"alice\") :ok = Wiregrid.watch_presence(:chat, session_id, \"bob\") :ok = Wiregrid.unwatch_presence(:chat, session_id, \"bob\") Built-in statuses are :onli"},
  {href: "storage.html", title: "Storage", text: "Adapters Storage Wiregrid.Storage is an append-oriented stream interface. The behaviour requires bootstrap/1 , append/6 , get/3 , page/4 , delete/3 , prune/4 , and health/1 . Streams are validated topics. Ids are binaries bounded by max_storage_id_bytes (256). A page is ordered by (inserted_at_ms, id) so two rows with the same millisecond are neither skipped nor repeated. The cursor is an opaque binary from Wiregrid.Storage.Cursor . The application chooses what is persisted. publish(..., persist: true) appends after validation and authorization and before local fanout. Wiregrid.Chat.say/4 sets"},
  {href: "configuration.html", title: "Configuration", text: "Reference Configuration An instance is configured at start. There is no required application environment. Wiregrid.Config.build/1 rejects a non-keyword, a list longer than 128 entries, duplicate keys ( :invalid_options ), and unknown keys ( {:unknown_options, keys} ). The profile is applied first, then explicit options overwrite it. Wiregrid.start_instance(:chat, profile: :balanced, max_sessions: 4_000) Profiles :small is the conservative set for development and modest services. :balanced is the default and the usual production start. :large raises ceilings. It does not preallocate the tables,"},
  {href: "security.html", title: "Authorization and security", text: "Boundary Authorization and security Wiregrid bounds its own runtime. The embedding application decides who a user is and what that user may do. Identity, TLS, database credentials, network segmentation, distribution cookies, backups, and product abuse rules stay outside the library. The authorizer callback defmodule MyApp.Auth do @behaviour Wiregrid.Authorizer @impl true def authorize(action, session, resource, context) do if MyApp.Policy.allow?(session.user_id, action, resource, context), do: :ok, else: {:error, :unauthorized} end end Wiregrid.start_instance(:chat, authorizer: MyApp.Auth, sec"},
  {href: "elixir.html", title: "Elixir API", text: "Elixir Elixir API Wiregrid is the stable façade. Application code should call it, or Wiregrid.Chat , rather than ETS tables or internal process names. Results that can fail are tagged tuples. Session ids, resume tokens, prepared handles, dispatch plans, and cursors are opaque. Discovery Wiregrid.version() Wiregrid.protocol_version() # 1 Wiregrid.foreign_protocol_version() # 1 Wiregrid.c_abi_major() # 1 {:ok, caps} = Wiregrid.capabilities(:chat) Wiregrid.describe(:chat) Wiregrid.deployment_report(:chat) Wiregrid.limits(:chat) version/0 reads the application vsn and falls back to \"1.0.0\" . capab"},
  {href: "erlang.html", title: "Erlang API", text: "Erlang Erlang API src/wiregrid_api.erl is the plain-term boundary. Callers do not build Elixir structs, expand macros, or guess keyword defaults. Every option-bearing function has an explicit arity. New BEAM bindings should target this module. Gleam and LFE already do. {ok, _} = wiregrid_api:start_instance(chat), {ok, Sid} = wiregrid_api:connect(chat, <<\"alice\">>, self()), Topic = wiregrid_api:topic_channel(<<\"general\">>), ok = wiregrid_api:subscribe(chat, Sid, Topic), {ok, _} = wiregrid_api:publish(chat, Topic, #{type => message}). Deliveries use the same mailbox tuple, {'$wiregrid', Envelope"},
  {href: "gleam.html", title: "Gleam", text: "Gleam Gleam bindings bindings/gleam targets Erlang and calls wiregrid_api . There is no Gleam runtime and no second copy of session state. Closed types cover topics, targets, presence, signals, delivery class, profiles, prepared handles, dispatch plans, and actors. Event bodies and metadata stay Dynamic , because Wiregrid does not own the product schema. ./scripts/check-gleam.sh CI pins Gleam 1.18.1. WIREGRID_REQUIRE_GLEAM=1 makes a missing compiler fail the check. Types pub type Topic { User(String) Channel(String) Thread(String) Room(String) Game(String) Document(String) Custom(String, Strin"},
  {href: "lfe.html", title: "LFE", text: "LFE LFE bindings LFE is optional. Elixir and Erlang own sessions, ETS, resume tokens, storage rules, authorization, and delivery reservations. LFE calls wiregrid_api . The node boots with no LFE compiler installed. Compile the bindings with mix wiregrid.lfe.compile or ./scripts/check-lfe.sh . Release builds that require LFE should set WIREGRID_REQUIRE_LFE=1 . CI builds LFE 2.2.2. The sources are split by job. A lane that preserves per-key order has different failure rules from a coalesce step that may drop superseded typing events, and both differ from a macro that emits fixed topic constructo"},
  {href: "foreign.html", title: "C ABI and the gateway", text: "Protocol 1 C ABI and the foreign gateway native/c is a client library, not a NIF. It speaks TCP to Wiregrid.Foreign.Gateway . A fault in the C, Rust, Zig, Go, Python, Java, or .NET process stays in that process. The client never sees ETS table ids, pids, or Erlang external term layout. A subscribed C client on general is on the same channel as MyApp.Chat.join(session, \"general\") . Framing The socket is Erlang gen_tcp with packet: 4 : a 4-byte big-endian length, then one frame. Inside the frame: Bytes Field 1 version, currently 1 1 kind: 0 request, 1 response, 2 event 1 operation 4 request id, "},
  {href: "ui.html", title: "Browser UI kit", text: "Browser Browser UI kit The kit under priv/ui is CSS and small JavaScript modules. It does not own the socket, the store, the router, or the message schema. Public class names start with wg- . Utility classes start with wg-u- , so they can sit beside another utility set. Message text is written with textContent . There is no helper that assigns a message body to innerHTML . Install paths mix wiregrid.ui.install mix wiregrid.ui.install priv/static/vendor/wiregrid --force The default destination is priv/static/vendor/wiregrid . Existing files are left in place unless --force is passed. The task c"},
  {href: "cowboy.html", title: "Cowboy transport", text: "Transport Cowboy transport Wiregrid.Transport.Cowboy is an optional WebSocket handler. The host application supplies Cowboy. Wiregrid’s own mix.exs lists Cowboy only under only: :test , pinned to ~> 2.18.0 with Cowlib ~> 2.19.0 so OTP 26 can compile the suite. Production apps add the Cowboy version they run. Authentication runs in init/2 , before the upgrade. The session is created in websocket_init/1 , so the Cowboy process is the session owner. On terminate, a live session is disconnected with reason :transport_closed . Options Key Default :instance required :auth / :auth_fun how the request"},
  {href: "operations.html", title: "Clustering and operations", text: "Operations Clustering and operations Clustering is off by default ( cluster: false ). Turn it on only when distribution is already a private network with a cookie and, where you need it, TLS distribution. Peers are trusted. Do not publish EPMD or the distribution port. What clustering does Wiregrid.start_instance(:chat, profile: :balanced, cluster: true, cluster_shards: 16, cluster_namespace: :chat, cluster_dedupe_ttl_ms: 60_000, cluster_pending_per_shard: 10_000 ) Enabled clustering uses a fixed shard count and fixed :pg groups. It does not create a process group per topic. Local ETS remains "},
  {href: "architecture.html", title: "Architecture", text: "Internals Architecture Lifecycle work and fanout work are separate. Wiregrid.Runtime serializes changes that move several indexes together: sessions, subscriptions, rooms, presence watches, resume snapshots, and drain state. One runtime process per instance keeps those ETS updates ordered. Publishing does not enter that process. Wiregrid.Fanout reads the indexes and walks recipients in bounded batches, so a busy channel does not have to wait behind session setup. Processes and tables Wiregrid.Tables owns the per-instance ETS tables and is the place Wiregrid.Tables.get/1 reads config from. Wire"},
];
